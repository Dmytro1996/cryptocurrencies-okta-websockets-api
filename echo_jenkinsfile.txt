pipeline {
    agent any
    triggers {
        cron(env.BRANCH_NAME=='master'?'* * H * *':'')
    }
    stages {
        stage ("Echo command"){
            steps {
                echo "Hello, Dmytro!"
            }
        }
    }
}