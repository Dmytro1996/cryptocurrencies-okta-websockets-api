pipeline {
    agent any
    triggers {
        cron(env.BRANCH_NAME=='master'?'H/2 * * * *':'')
    }
    stages {
        stage ("Echo command"){
            steps {
                echo "Hello, Dmytro!"
            }
        }
    }
}