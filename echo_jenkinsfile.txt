pipeline {
    agent any
    triggers {
        cron(env.BRANCH_NAME=='master'?'0 0 H * *':'')
    }
    stages {
        stage ("Echo command"){
            steps {
                echo "Hello, Dmytro!"
            }
        }
    }
}